[project]
name = "authenticationpractice"
version = "0.1.0"
description = "FastAPI API built to practice web authentication mechanisms"
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard]>=0.128.8",
]
